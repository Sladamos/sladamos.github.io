import { DolarPriceFormatterPipe } from './dolar-price-formatter.pipe';

describe('DolarPriceFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new DolarPriceFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
