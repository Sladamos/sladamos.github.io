import { DecimalNumberFormatterPipe } from './decimal-number-formatter.pipe';

describe('DecimalNumberFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalNumberFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
